function CalcImageHistogram(imageArray){

    var histRed = Array.apply(null,  Array(255)).map(Number.prototype.valueOf,0);
    var histGreen = Array.apply(null,  Array(255)).map(Number.prototype.valueOf,0);
    var histBlue = Array.apply(null,  Array(255)).map(Number.prototype.valueOf,0);

    
}